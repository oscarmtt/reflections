What happens when you initialize a repository? Why do you need to do it?



You create the repository.
For creating a commit.


How is the staging area different from the working directory and the repository?
What value do you think it offers?



The staging area is like an intermediate section.

The value is similar to the windows clipboard.


How can you use the staging area to make sure you have one commit per logical change?

Adding only the necessary things to  commit


What are some situations when branches would be helpful in keeping your history organized? How would branches help?

When you try to create an experimental/alternative to your code.
Branches let you work on different versions/branches/alternatives of your code, without altering the main one.


How do the diagrams help you visualize the branch structure?

A visual representation is always helpful for understanding the concept.


What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

The two alternatives of the code merge/join into only one piece of code
Because it creates in the end one chain of commits, instead of two chains.


What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

Pros: faster and easier
Cons: you may occur into some problemas and